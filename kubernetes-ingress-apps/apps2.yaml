apiVersion: v1
kind: Pod
metadata:
  name: app2
  labels:
    app.kubernetes.io/name: app2
spec:
  containers:
  - name: app2
    image: prateekkumawat/ingress-example:app2
    resources:
      requests:
        memory: "100Mi"
        cpu: "300m" 
      limits:
        memory: "128Mi"     
        cpu: "500m"
    ports:
    - containerPort: 80
      name: http

---
apiVersion: v1
kind: Service
metadata:
  name: app2
spec:
  selector:
    app.kubernetes.io/name: app2
  ports:
  - port: 80
    targetPort: 80
